// src/app/dashboard/page.tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Progress } from "@/components/ui/progress";
import { 
  Users, 
  UserCheck, 
  Shield, 
  Key, 
  Database, 
  Layers, 
  Activity, 
  AlertTriangle,
  TrendingUp,
  TrendingDown,
  Clock,
  CheckCircle,
  XCircle
} from "lucide-react";

// Componente mejorado de estadísticas con iconos
function ModernStat({ 
  title, 
  value, 
  subtitle, 
  icon: Icon, 
  trend, 
  color = "default" 
}: { 
  title: string; 
  value: string | number; 
  subtitle?: string; 
  icon: any;
  trend?: { value: number; isPositive: boolean };
  color?: "default" | "success" | "warning" | "danger";
}) {
  const colorClasses = {
    default: "text-muted-foreground",
    success: "text-green-600",
    warning: "text-yellow-600", 
    danger: "text-red-600"
  };

  return (
    <Card className="relative overflow-hidden transition-all duration-200 hover:shadow-md border-0 shadow-sm">
      <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
        <CardTitle className="text-sm font-medium text-muted-foreground">{title}</CardTitle>
        <Icon className={`h-4 w-4 ${colorClasses[color]}`} />
      </CardHeader>
      <CardContent>
        <div className="flex items-baseline space-x-2">
          <div className="text-2xl font-bold leading-none">{value}</div>
          {trend && (
            <div className={`flex items-center text-xs ${trend.isPositive ? 'text-green-600' : 'text-red-600'}`}>
              {trend.isPositive ? <TrendingUp className="h-3 w-3" /> : <TrendingDown className="h-3 w-3" />}
              <span className="ml-1">{Math.abs(trend.value)}%</span>
            </div>
          )}
        </div>
        {subtitle && <div className="text-xs text-muted-foreground mt-1 leading-relaxed">{subtitle}</div>}
      </CardContent>
      <div className={`absolute bottom-0 left-0 right-0 h-1 ${
        color === 'success' ? 'bg-green-100' : 
        color === 'warning' ? 'bg-yellow-100' : 
        color === 'danger' ? 'bg-red-100' : 'bg-muted/20'
      }`} />
    </Card>
  );
}

// Componente de gráfico de barras mejorado
function EnhancedBarChart({ 
  title, 
  data, 
  description 
}: { 
  title: string; 
  data: { label: string; value: number }[]; 
  description?: string;
}) {
  const max = Math.max(1, ...data.map((d) => d.value));
  const barWidth = 32;
  const gap = 20;
  const width = data.length * (barWidth + gap) + gap;
  const height = 180;
  
  return (
    <Card className="transition-all duration-200 hover:shadow-md border-0 shadow-sm">
      <CardHeader>
        <div className="flex items-center justify-between">
          <div>
            <CardTitle className="text-lg font-semibold">{title}</CardTitle>
            {description && <p className="text-sm text-muted-foreground mt-1">{description}</p>}
          </div>
          <Badge variant="outline" className="text-xs">
            Total: {data.reduce((acc, d) => acc + d.value, 0)}
          </Badge>
        </div>
      </CardHeader>
      <CardContent>
        <div className="overflow-x-auto">
          <svg width={width} height={height} className="text-primary">
            {data.map((d, i) => {
              const h = Math.round((d.value / max) * (height - 40));
              const x = gap + i * (barWidth + gap);
              const y = height - h - 20;
              return (
                <g key={d.label}>
                  <rect 
                    x={x} 
                    y={y} 
                    width={barWidth} 
                    height={h} 
                    rx={6} 
                    className="fill-current opacity-80 hover:opacity-100 transition-opacity"
                  />
                  <text 
                    x={x + barWidth / 2} 
                    y={y - 8} 
                    textAnchor="middle" 
                    className="fill-foreground text-xs font-medium"
                  >
                    {d.value}
                  </text>
                  <text 
                    x={x + barWidth / 2} 
                    y={height - 4} 
                    textAnchor="middle" 
                    className="fill-muted-foreground text-[10px]"
                  >
                    {d.label}
                  </text>
                </g>
              );
            })}
          </svg>
        </div>
    </Card>
  );
}

// Componente de gráfico de líneas mejorado
function EnhancedLineChart({ 
  title, 
  series, 
  labels, 
  description 
}: { 
  title: string; 
  series: number[]; 
  labels: string[]; 
  description?: string;
}) {
  const w = 440;
  const h = 180;
  const pad = 20;
  const max = Math.max(1, ...series);
  const stepX = (w - pad * 2) / Math.max(1, series.length - 1);
  const points = series
    .map((v, i) => {
      const x = pad + i * stepX;
      const y = h - pad - (v / max) * (h - pad * 2);
      return `${x},${y}`;
    })
    .join(" ");

  const avg = series.reduce((a, b) => a + b, 0) / series.length;
    
  return (
    <Card className="transition-all duration-200 hover:shadow-md border-0 shadow-sm">
      <CardHeader>
        <div className="flex items-center justify-between">
          <div>
            <CardTitle className="text-lg font-semibold">{title}</CardTitle>
            {description && <p className="text-sm text-muted-foreground mt-1">{description}</p>}
          </div>
          <div className="text-right">
            <div className="text-xs text-muted-foreground">Promedio</div>
            <div className="text-sm font-medium">{Math.round(avg)}</div>
          </div>
        </div>
      </CardHeader>
      <CardContent>
        <div className="overflow-x-auto">
          <svg width={w} height={h} className="text-primary">
            <defs>
              <linearGradient id="gradient" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stopColor="currentColor" stopOpacity="0.1"/>
                <stop offset="100%" stopColor="currentColor" stopOpacity="0"/>
              </linearGradient>
            </defs>
            <path
              d={`M ${points.split(' ')[0]} L ${points} L ${pad + (series.length - 1) * stepX},${h - pad} L ${pad},${h - pad} Z`}
              fill="url(#gradient)"
            />
            <polyline
              points={points}
              fill="none"
              stroke="currentColor"
              strokeWidth="3"
              strokeLinejoin="round"
              strokeLinecap="round"
              className="drop-shadow-sm"
            />
            {series.map((v, i) => {
              const x = pad + i * stepX;
              const y = h - pad - (v / max) * (h - pad * 2);
              return (
                <circle
                  key={i}
                  cx={x}
                  cy={y}
                  r="4"
                  fill="currentColor"
                  className="drop-shadow-sm"
                />
              );
            })}
            <line x1={pad} y1={h - pad} x2={w - pad} y2={h - pad} className="stroke-muted-foreground/20" strokeDasharray="2,2" />
            {labels.map((l, i) => (
              <text
                key={l + i}
                x={pad + i * stepX}
                y={h - 2}
                textAnchor="middle"
                className="fill-muted-foreground text-[10px] font-medium"
              >
                {l}
              </text>
            ))}
          </svg>
        </div>
      </CardContent>
    </Card>
  );
}

export default function DashboardPage() {
  const mainStats = [
    { 
      title: "Total Usuarios", 
      value: 245, 
      icon: Users, 
      trend: { value: 12, isPositive: true },
      subtitle: "↑ 12% desde el mes pasado"
    },
    { 
      title: "Usuarios Activos", 
      value: 198, 
      icon: UserCheck, 
      color: "success" as const,
      trend: { value: 8, isPositive: true },
      subtitle: "81% de tasa de actividad"
    },
    { 
      title: "Sesiones Activas", 
      value: 21, 
      icon: Activity, 
      color: "default" as const,
      subtitle: "En tiempo real"
    },
    { 
      title: "Intentos Fallidos", 
      value: 17, 
      icon: AlertTriangle, 
      color: "warning" as const,
      subtitle: "Últimas 24 horas"
    }
  ];

  const systemStats = [
    { title: "Roles Configurados", value: 32, icon: Shield, color: "default" as const },
    { title: "Permisos Totales", value: 476, icon: Key, color: "default" as const },
    { title: "Objetos del Sistema", value: 154, icon: Database, color: "default" as const },
    { title: "Aplicaciones", value: 4, icon: Layers, color: "success" as const }
  ];

  const permisosPorApp = [
    { label: "SEC", value: 210 },
    { label: "SGM", value: 120 },
    { label: "GOYA", value: 92 },
    { label: "SYS", value: 54 }
  ];

  const accesosDiarios = [14, 22, 19, 31, 28, 35, 27];
  const dias = ["Lun", "Mar", "Mié", "Jue", "Vie", "Sáb", "Dom"];

  return (
    <div className="p-6 space-y-8 bg-gradient-to-br from-background to-muted/20 min-h-screen">
      {/* Header mejorado */}
      <div className="space-y-2">
        <div className="flex items-center justify-between">
          <div>
            <h1 className="text-3xl font-bold tracking-tight">Panel de Control</h1>
            <p className="text-muted-foreground">Sistema de Seguridad y Gestión de Accesos</p>
          </div>
          <div className="flex items-center space-x-2">
            <Badge variant="outline" className="bg-green-50 text-green-700 border-green-200">
              <CheckCircle className="w-3 h-3 mr-1" />
              Sistema Activo
            </Badge>
            <div className="text-sm text-muted-foreground">
              <Clock className="w-4 h-4 inline mr-1" />
              Actualizado hace 2 min
            </div>
          </div>
        </div>
      </div>

      {/* Métricas principales */}
      <div className="space-y-4">
        <h2 className="text-xl font-semibold text-foreground">Métricas Principales</h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {mainStats.map((stat) => (
            <ModernStat key={stat.title} {...stat} />
          ))}
        </div>
      </div>

      {/* Gráficos principales */}
      <div className="grid grid-cols-1 xl:grid-cols-2 gap-6">
        <EnhancedLineChart 
          title="Tendencia de Accesos" 
          series={accesosDiarios} 
          labels={dias}
          description="Actividad de usuarios en los últimos 7 días"
        />
        <EnhancedBarChart 
          title="Distribución de Permisos" 
          data={permisosPorApp}
          description="Permisos asignados por aplicación"
        />
      </div>

      {/* Métricas del sistema */}
      <div className="space-y-4">
        <h2 className="text-xl font-semibold text-foreground">Estado del Sistema</h2>
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {systemStats.map((stat) => (
            <ModernStat key={stat.title} {...stat} />
          ))}
        </div>
      </div>

      {/* Panel de alertas y accesos rápidos */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <Card className="transition-all duration-200 hover:shadow-md border-0 shadow-sm">
          <CardHeader>
            <CardTitle className="text-lg font-semibold">Alertas Recientes</CardTitle>
          </CardHeader>
          <CardContent className="space-y-3">
            <div className="flex items-center space-x-3 p-3 rounded-lg bg-yellow-50 border border-yellow-200">
              <AlertTriangle className="h-4 w-4 text-yellow-600" />
              <div className="flex-1 text-sm">
                <div className="font-medium">12 usuarios sin rol asignado</div>
                <div className="text-muted-foreground">Revisar asignaciones pendientes</div>
              </div>
            </div>
            <div className="flex items-center space-x-3 p-3 rounded-lg bg-blue-50 border border-blue-200">
              <Database className="h-4 w-4 text-blue-600" />
              <div className="flex-1 text-sm">
                <div className="font-medium">8 permisos obsoletos detectados</div>
                <div className="text-muted-foreground">Considerar limpieza de permisos</div>
              </div>
            </div>
            <div className="flex items-center space-x-3 p-3 rounded-lg bg-red-50 border border-red-200">
              <XCircle className="h-4 w-4 text-red-600" />
              <div className="flex-1 text-sm">
                <div className="font-medium">5 objetos sin relación</div>
                <div className="text-muted-foreground">Requiere vinculación inmediata</div>
              </div>
            </div>
          </CardContent>
        </Card>

        <Card className="transition-all duration-200 hover:shadow-md border-0 shadow-sm">
          <CardHeader>
            <CardTitle className="text-lg font-semibold">Accesos Rápidos</CardTitle>
          </CardHeader>
          <CardContent className="space-y-3">
            <button className="w-full text-left p-3 rounded-lg border border-muted hover:bg-muted/50 transition-colors">
              <div className="flex items-center space-x-3">
                <Users className="h-4 w-4 text-muted-foreground" />
                <div>
                  <div className="font-medium text-sm">Gestionar Usuarios</div>
                  <div className="text-xs text-muted-foreground">Crear, editar y administrar usuarios</div>
                </div>
              </div>
            </button>
            <button className="w-full text-left p-3 rounded-lg border border-muted hover:bg-muted/50 transition-colors">
              <div className="flex items-center space-x-3">
                <Shield className="h-4 w-4 text-muted-foreground" />
                <div>
                  <div className="font-medium text-sm">Configurar Roles</div>
                  <div className="text-xs text-muted-foreground">Definir perfiles y permisos</div>
                </div>
              </div>
            </button>
            <button className="w-full text-left p-3 rounded-lg border border-muted hover:bg-muted/50 transition-colors">
              <div className="flex items-center space-x-3">
                <Activity className="h-4 w-4 text-muted-foreground" />
                <div>
                  <div className="font-medium text-sm">Ver Eventos</div>
                  <div className="text-xs text-muted-foreground">Monitorear actividad del sistema</div>
                </div>
              </div>
            </button>
          </CardContent>
        </Card>
      </div>
    </div>
  );
}